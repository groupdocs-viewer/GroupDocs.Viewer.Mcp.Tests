services:
  groupdocs-viewer-mcp:
    image: ghcr.io/groupdocs-viewer/viewer-net-mcp:latest
    stdin_open: true
    tty: false
    environment:
      GROUPDOCS_MCP_STORAGE_PATH: /data
    volumes:
      - ./documents:/data
